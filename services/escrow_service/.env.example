# Escrow Service Environment Variables

# Set to "true" to use real Stripe API instead of mock connector
USE_REAL_STRIPE=false

# Stripe API keys (only needed if USE_REAL_STRIPE=true)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...

# Service port
PORT=8004